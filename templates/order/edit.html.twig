{% extends 'base.html.twig' %}

{% block title %}{{ 'app.order.edit.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h2>{{ 'app.order.edit.heading'|trans({'id': order.id}) }}</h2>

	{{ include('order/_form.html.twig') }}

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('order_index') }}" class="btn btn-secondary">{{ 'global.action.list'|trans }}</a>
		</li>
		<li class="list-inline-item">
			{{ include('order/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}
