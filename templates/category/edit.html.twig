{% extends 'base.html.twig' %}

{% block title %}{{ category.title }} | {{ parent() }}{% endblock %}

{% block body %}
	<h2>{{ category.title }}{% if category.colour %}
		<span class="badge px-1" style="background-color: {{ category.colour }} !important;">{{ category.label }}</span>{% endif %}</h2>

	{{ include('category/_form.html.twig') }}

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('category_index') }}" class="btn btn-secondary" title="{{ 'global.action.list'|trans }}" aria-label="{{ 'global.action.list'|trans }}">📃</a>
		</li>
		<li class="list-inline-item">
			{{ include('category/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}

{% block javascripts %}
		<script src="{{ asset('js/coloured_label.js')}}"></script>
{% endblock %}
